/**
 * this is a global reference to modules that should be accessable to user scrips 
 */

const drawsocket = require('./drawsocket_wrapper.js') // drawsocket must be before the renderer
const symbolist = require('./symbolist-renderer')

